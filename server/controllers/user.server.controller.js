// grab the user model
var User = require('../models/user.model');



exports.addUser = (res, req) => {
    // create a new user
    //var newUser = User();

    console.log(req.body);

    // save the user
  /*  newUser.save(function(err) {
    if (err) throw console.log(err);

    console.log('User created!');
    });*/
}